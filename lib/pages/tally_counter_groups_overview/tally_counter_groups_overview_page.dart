import 'package:flutter/widgets.dart';

class TallyCounterGroupsOverviewPage extends StatelessWidget {
  const TallyCounterGroupsOverviewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Hello"),
      ),
    );
  }
}
